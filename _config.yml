# GitHub Pages Configuration
title: Quantumult X Rules
description: CN Direct + GFW Proxy + Ad Block Rules
lang: en

# Keep these files in output
include: [".list", "filter"]
keep_files: ["filter"]

# Disable Jekyll processing
plugins: []
markdown: kramdown
